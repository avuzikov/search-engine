import React from "react";
import SearchBox from "../requestComponents/SearchBox";

const ToDoListPage = () => {
  return (
    <React.Fragment>
      <SearchBox />
    </React.Fragment>
  );
};

export default ToDoListPage;
